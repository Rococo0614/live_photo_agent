name: live-photo-agent-cpu
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - ffmpeg
  - pip:
      - -e .[dev]
      - torch==2.5.1
      - transformers==4.46.3
      - accelerate>=0.34,<1.0
